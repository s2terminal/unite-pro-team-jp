---
import Layout from '../layouts/Layout.astro';
import { getTeams } from '../lib/models';
import TeamList from '../components/pages/TeamList';

const teams = await getTeams();
---

<Layout>
  <TeamList client:visible teams={teams} />
</Layout>
